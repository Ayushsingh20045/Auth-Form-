<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <link rel="stylesheet" href="style.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap');

* { margin: 0; padding: 0; box-sizing: border-box; }
*:focus { outline: none; }
body {
    width: 100%;
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background-image: url('https://i.postimg.cc/nz6YzMPg/background.jpgs');
    background-size: cover;
    font-family: 'Roboto';
}
.container { 
    width: 800px; 
    height: 500px; 
    display: flex; 
    justify-content: center; 
    align-items: center; 
    background: #FFF; 
    box-shadow: 0 0 10px rgba(0, 0, 0, .1); 
    overflow: hidden; 
    position: relative; 
    border: 10px solid #FFF; 
    border-radius: 10px; 
}
.bg { 
    width: 50%; 
    height: 100%; 
    background-image: url('https://i.postimg.cc/nz6YzMPg/background.jpgs'); 
    background-size: cover; 
    border-top-left-radius: 5px; 
    border-bottom-left-radius: 5px; 
    position: relative; 
}
    .bg::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 2;
        background: linear-gradient(90deg, rgba(255, 255, 255, 0) 0%, rgba(255, 140, 0, .5) 100%);
    }
form { min-width: 250px !important; width: 50%; height: 100%; padding: 30px 20px; }
.heading { font-size: 40px; text-transform: capitalize; margin-bottom: 60px; text-align: center; }
label { font-size: 16px; display: block; margin: 10px 0; text-transform: capitalize; }
input {font-size: 16px; display: block; width: 90%; border: none; border-bottom: 1px solid #000; margin-bottom: 20px; }
.login-btn {
    width: 140px;
    height: 40px;
    text-transform: capitalize;
    border-radius: 50px;
    border: none;
    background: orange;
    color: #FFF;
    display: block;
    cursor: pointer;
    margin: 50px auto;
    font-size: 20px;
}
.register-btn { font-size: 14px; color: rgb(117, 117, 117); text-transform: capitalize; bottom: 20px; right: 20px; cursor: pointer; margin-top: 10px;  position: absolute; bottom: 0px; right: 90px;}

@media screen and (max-width: 750px) {
    .container { width: 350px; }
    .bg { display: none; }
    form { display: 100%; }
}
.google {
    text-decoration: none;
    padding: 20px 62px;
    border: 1px solid black;
    color: #000;
    border-radius: 30px;

  display: flex;
  justify-content: center;
  align-items: center;
  
}
.google i{
    color: blue;
}
    </style>
</head>
<body>
    <div class="container">
        <div class="bg"></div>
        <form action="/auth/login" method="post">
            <h2 class="heading">Login Here</h2>

            <label for="email">Email: </label>
            <input type="text" name="email">

            <label for="password">Password: </label>
            <input type="password" name="password" >

            <button class="login-btn" type="submit">Login</button>

           <a class="google" href="/auth/google"><i class="ri-google-fill"></i> Sign up with Google</a>

            <span class="register-btn">Don't have an account?<a href="/auth/register">Register</a></span>
        </form>
    </div>
</body>
</html>
